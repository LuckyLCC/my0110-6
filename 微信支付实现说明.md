# 微信支付实现说明

## 前端实现

### 已完成的实现

1. **API 接口封装** (`api/request.js`)
   - 添加了 `api.payment.createPackageOrder` - 创建会员套餐支付订单
   - 添加了 `api.payment.getWechatPayParams` - 获取微信支付参数

2. **支付页面实现** (`pages/payment/payment.vue`)
   - 实现了完整的微信支付流程
   - 包含登录检查、错误处理、支付成功/失败回调

### 支付流程

```
用户点击"立即支付"
    ↓
检查是否登录
    ↓
调用后端创建支付订单
    ↓
获取微信支付参数
    ↓
调起微信支付 (uni.requestPayment)
    ↓
处理支付结果
    ↓
支付成功 → 跳转到"我的"页面
支付失败/取消 → 显示错误提示
```

## 后端需要实现的接口

### 1. 创建会员套餐支付订单

**接口：** `POST /api/payment/create-package-order`

**请求头：**
```
Authorization: Bearer {token}
Content-Type: application/json
```

**请求参数：**
```json
{
  "packageId": 1,
  "price": 3980
}
```

**响应格式：**
```json
{
  "code": 200,
  "message": "订单创建成功",
  "data": {
    "orderId": 123,
    "orderNo": "ORDER20240113123456",
    "packageId": 1,
    "packageName": "月卡",
    "price": 3980,
    "status": "unpaid"
  }
}
```

### 2. 获取微信支付参数

**接口：** `POST /api/payment/wechat-pay/{orderId}`

**请求头：**
```
Authorization: Bearer {token}
```

**路径参数：**
- `orderId`: 订单ID

**响应格式：**
```json
{
  "code": 200,
  "message": "获取支付参数成功",
  "data": {
    "timeStamp": "1609459200",
    "nonceStr": "5K8264ILTKCH16CQ2502SI8ZNMTM67VS",
    "package": "prepay_id=wx2016121016420242444321ca0631331346",
    "signType": "RSA",
    "paySign": "oR9d8PuhnIc+YZ8cBHFCwfgpaK9gd7vaRvqDzHc4c..."
  }
}
```

## 后端实现示例（Java Spring Boot）

### 1. 创建 PaymentController

```java
package com.oxygen.capsule.controller;

import com.oxygen.capsule.common.Result;
import com.oxygen.capsule.service.PaymentService;
import com.oxygen.capsule.util.JwtUtil;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.*;

import java.util.Map;

@RestController
@RequestMapping("/api/payment")
public class PaymentController {

    @Autowired
    private PaymentService paymentService;

    @Autowired
    private JwtUtil jwtUtil;

    // 创建会员套餐支付订单
    @PostMapping("/create-package-order")
    public Result<Map<String, Object>> createPackageOrder(
            @RequestHeader("Authorization") String token,
            @RequestBody Map<String, Object> params) {
        
        if (token == null || !token.startsWith("Bearer ")) {
            return Result.error("未提供有效的认证令牌");
        }

        Long userId = jwtUtil.getUserIdFromToken(token.substring(7));
        Long packageId = Long.valueOf(params.get("packageId").toString());
        Double price = Double.valueOf(params.get("price").toString());

        // 创建订单
        Map<String, Object> order = paymentService.createPackageOrder(userId, packageId, price);
        
        return Result.success("订单创建成功", order);
    }

    // 获取微信支付参数
    @PostMapping("/wechat-pay/{orderId}")
    public Result<Map<String, String>> getWechatPayParams(
            @RequestHeader("Authorization") String token,
            @PathVariable Long orderId) {
        
        if (token == null || !token.startsWith("Bearer ")) {
            return Result.error("未提供有效的认证令牌");
        }

        Long userId = jwtUtil.getUserIdFromToken(token.substring(7));
        
        // 验证订单归属
        // 获取微信支付参数
        Map<String, String> payParams = paymentService.getWechatPayParams(orderId, userId);
        
        return Result.success("获取支付参数成功", payParams);
    }
}
```

### 2. 微信支付参数生成

需要使用微信支付 SDK（如 WxPay）来生成支付参数：

```java
// 示例：使用微信支付统一下单接口
public Map<String, String> getWechatPayParams(Long orderId, Long userId) {
    // 1. 查询订单信息
    Order order = orderService.findById(orderId);
    
    // 2. 调用微信支付统一下单接口
    // 需要使用微信支付商户号和密钥
    String appId = "你的小程序AppID";
    String mchId = "你的商户号";
    String apiKey = "你的API密钥";
    
    // 3. 构建统一下单请求
    Map<String, String> requestParams = new HashMap<>();
    requestParams.put("appid", appId);
    requestParams.put("mch_id", mchId);
    requestParams.put("nonce_str", generateNonceStr());
    requestParams.put("body", order.getPackageName());
    requestParams.put("out_trade_no", order.getOrderNo());
    requestParams.put("total_fee", String.valueOf((int)(order.getPrice() * 100))); // 转换为分
    requestParams.put("spbill_create_ip", getClientIp());
    requestParams.put("notify_url", "https://your-domain.com/api/payment/notify");
    requestParams.put("trade_type", "JSAPI");
    requestParams.put("openid", user.getOpenid());
    
    // 4. 生成签名
    String sign = generateSign(requestParams, apiKey);
    requestParams.put("sign", sign);
    
    // 5. 调用微信支付接口获取 prepay_id
    String prepayId = callWechatPayAPI(requestParams);
    
    // 6. 生成小程序支付参数
    Map<String, String> payParams = new HashMap<>();
    payParams.put("timeStamp", String.valueOf(System.currentTimeMillis() / 1000));
    payParams.put("nonceStr", generateNonceStr());
    payParams.put("package", "prepay_id=" + prepayId);
    payParams.put("signType", "RSA"); // 或 "MD5"，根据微信支付版本
    payParams.put("paySign", generatePaySign(payParams, appId, apiKey));
    
    return payParams;
}
```

## 微信支付配置

### 1. 申请微信支付商户号

1. 访问 [微信支付商户平台](https://pay.weixin.qq.com/)
2. 注册并申请商户号
3. 完成资质审核

### 2. 配置支付参数

在 `application.yml` 中配置：

```yaml
wechat:
  pay:
    appid: 你的小程序AppID
    mchid: 你的商户号
    apiKey: 你的API密钥
    certPath: 证书路径（如果需要）
    notifyUrl: https://your-domain.com/api/payment/notify
```

### 3. 配置支付域名

在微信支付商户平台配置：
- **支付授权目录**：你的小程序域名
- **支付回调URL**：你的后端回调地址

## 支付回调处理

### 支付成功回调接口

**接口：** `POST /api/payment/notify`

**说明：**
- 微信支付成功后，会异步通知这个接口
- 需要验证签名，确保请求来自微信
- 更新订单状态为已支付
- 返回 `success` 给微信

## 注意事项

1. **价格单位**
   - 微信支付接口要求价格以"分"为单位
   - 前端传递的是"元"，后端需要转换为"分"

2. **签名算法**
   - 微信支付使用 RSA 或 MD5 签名
   - 必须严格按照微信文档生成签名

3. **支付环境**
   - `uni.requestPayment` 只能在微信小程序环境中使用
   - H5 环境需要使用其他支付方式

4. **错误处理**
   - 支付取消：用户主动取消支付
   - 支付失败：网络错误或其他原因
   - 需要给用户明确的提示

5. **订单状态**
   - 创建订单后状态为 `unpaid`
   - 支付成功后更新为 `paid`
   - 需要记录支付时间

## 测试步骤

1. **开发环境测试**
   - 使用微信支付沙箱环境
   - 配置测试商户号和密钥

2. **正式环境测试**
   - 使用真实商户号
   - 使用真实金额（建议使用小额测试）

3. **支付流程测试**
   - 测试正常支付流程
   - 测试支付取消
   - 测试支付失败
   - 测试支付回调

## 常见问题

**Q: 支付参数错误？**
A: 检查签名算法、时间戳格式、nonceStr 生成等

**Q: 支付调起失败？**
A: 检查小程序 AppID 是否与商户号绑定

**Q: 支付成功但订单未更新？**
A: 检查支付回调接口是否正常工作

**Q: 在 H5 环境无法支付？**
A: 微信支付只能在微信小程序环境中使用，H5 需要使用其他支付方式

