# 小程序启动指南（解决 app.json 错误）

## 问题原因

错误提示：`app.json: 在项目根目录未找到 app.json`

这是因为：
1. uni-app 项目需要先编译才能生成小程序的 `app.json` 等文件
2. 编译后的文件在 `unpackage/dist/dev/mp-weixin` 目录
3. 需要先在 HBuilderX 中编译，才能在微信开发者工具中打开

## ✅ 正确步骤（使用 HBuilderX）

### 方法一：使用 HBuilderX 一键启动（推荐）

1. **打开 HBuilderX**
   - 确保项目已打开：`/Users/liuchang/Downloads/my0110-6`

2. **编译并运行小程序**
   - 点击顶部菜单：**运行 → 运行到小程序模拟器 → 微信开发者工具**
   - HBuilderX 会自动：
     - ✅ 编译 uni-app 代码为小程序代码
     - ✅ 生成 `app.json`、`pages` 等小程序文件
     - ✅ 自动打开微信开发者工具

3. **首次配置微信开发者工具路径（如果提示）**
   - HBuilderX 会提示输入微信开发者工具安装路径
   - macOS 默认路径：`/Applications/wechatwebdevtools.app`
   - 输入路径后，HBuilderX 会自动打开微信开发者工具

### 方法二：手动编译后打开

如果方法一不行，手动编译：

1. **在 HBuilderX 中编译**
   - 点击菜单：**运行 → 运行到小程序模拟器 → 微信开发者工具**
   - 等待编译完成（控制台会显示编译进度）

2. **检查编译输出**
   - 编译后的文件在：`unpackage/dist/dev/mp-weixin/`
   - 应该包含：
     - `app.json` ✅
     - `app.js` ✅
     - `pages/` 目录 ✅
     - `static/` 目录 ✅

3. **在微信开发者工具中打开**
   - 打开微信开发者工具
   - 选择 **导入项目**
   - 项目目录：`/Users/liuchang/Downloads/my0110-6/unpackage/dist/dev/mp-weixin`
   - **重要**：不要选择项目根目录，要选择 `unpackage/dist/dev/mp-weixin` 目录
   - AppID：选择 **测试号**（或填写你的小程序 AppID）
   - 项目名称：城市森林氧舱
   - 点击 **导入**

## ❌ 错误做法

**不要直接打开项目根目录！**

错误的目录：
- ❌ `/Users/liuchang/Downloads/my0110-6` （项目根目录）
- ❌ `/Users/liuchang/Downloads/my0110-6/pages` （pages 目录）

正确的目录：
- ✅ `/Users/liuchang/Downloads/my0110-6/unpackage/dist/dev/mp-weixin` （编译后的目录）

## 验证编译是否成功

运行以下命令检查编译后的文件：

```bash
cd /Users/liuchang/Downloads/my0110-6
ls -la unpackage/dist/dev/mp-weixin/
```

应该看到：
```
app.json        # ✅ 小程序配置文件
app.js          # ✅ 小程序入口文件
pages/          # ✅ 页面目录
static/         # ✅ 静态资源
project.config.json  # 项目配置
```

如果这些文件不存在，说明编译未成功，需要在 HBuilderX 中重新编译。

## 常见问题

### Q1: HBuilderX 提示找不到微信开发者工具？

**A:** 手动指定路径：
1. HBuilderX → 工具 → 设置 → 运行配置
2. 微信开发者工具路径：`/Applications/wechatwebdevtools.app`

### Q2: 编译后还是没有 app.json？

**A:** 
1. 检查 HBuilderX 控制台是否有编译错误
2. 清理编译缓存：删除 `unpackage` 目录
3. 重新编译：运行 → 运行到小程序模拟器

### Q3: 微信开发者工具提示找不到 app.json？

**A:** 确保打开的是编译后的目录：
- 正确：`unpackage/dist/dev/mp-weixin`
- 错误：项目根目录或 pages 目录

### Q4: 页面显示不正常？

**A:** 
1. 检查 `pages.json` 配置是否正确
2. 检查图片路径（使用 `/static/` 开头）
3. 查看微信开发者工具控制台错误信息

## 推荐工作流程

1. ✅ 使用 HBuilderX 打开项目
2. ✅ 点击：运行 → 运行到小程序模拟器 → 微信开发者工具
3. ✅ 在微信开发者工具中预览和调试
4. ✅ 修改代码后，HBuilderX 会自动重新编译（如果开启了自动编译）

---

**总结：使用 HBuilderX 编译小程序是最可靠的方式！**

