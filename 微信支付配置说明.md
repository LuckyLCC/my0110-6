# 微信支付配置说明

## 错误：mch_id参数格式错误

### 问题原因

在 `application.yml` 中，`mchid`（商户号）被错误地设置为 AppID。

**错误配置：**
```yaml
wechat:
  pay:
    appid: wx1a44cbe925471245
    mchid: wx1a44cbe925471245  # ❌ 错误！这是 AppID，不是商户号
```

### 正确配置

**AppID 和商户号的区别：**

| 参数 | 格式 | 示例 | 说明 |
|------|------|------|------|
| **AppID** | 以 `wx` 开头的字符串 | `wx1a44cbe925471245` | 小程序的应用ID |
| **商户号（mchid）** | 8-10位纯数字 | `1234567890` | 微信支付商户号 |

### 正确配置示例

```yaml
wechat:
  miniapp:
    appid: wx1a44cbe925471245  # 小程序 AppID
    secret: 019d069035074ed1c45d1c35d69ed8c2  # 小程序 AppSecret
  pay:
    appid: wx1a44cbe925471245  # 小程序 AppID（与 miniapp.appid 相同）
    mchid: 1234567890  # ✅ 商户号（纯数字，8-10位）
    apiKey: your-api-key-here  # API密钥（32位字符串）
    certPath: classpath:cert/apiclient_cert.p12  # 证书路径（可选）
    notifyUrl: https://your-domain.com/api/payment/notify  # 支付回调URL
```

## 如何获取正确的配置值

### 1. AppID（小程序应用ID）

- 位置：微信公众平台 → 开发 → 开发管理 → 开发设置
- 格式：`wx` 开头的字符串
- 示例：`wx1a44cbe925471245`

### 2. 商户号（mchid）

- 位置：微信支付商户平台 → 账户中心 → 商户信息
- 格式：8-10位纯数字
- 示例：`1234567890`
- **重要**：这是商户号，不是 AppID！

### 3. API密钥（apiKey）

- 位置：微信支付商户平台 → 账户中心 → API安全 → API密钥
- 格式：32位字符串
- 示例：`019d069035074ed1c45d1c35d69ed8c2`
- **注意**：如果忘记密钥，需要重新设置（会影响所有使用该密钥的接口）

### 4. 支付回调URL（notifyUrl）

- 格式：必须是 HTTPS 协议
- 示例：`https://your-domain.com/api/payment/notify`
- **注意**：
  - 必须是公网可访问的 HTTPS 地址
  - 本地开发可以使用内网穿透工具（如 ngrok）

## 配置步骤

### 步骤 1：获取商户号

1. 登录 [微信支付商户平台](https://pay.weixin.qq.com/)
2. 进入"账户中心" → "商户信息"
3. 找到"商户号"，复制（通常是 8-10 位数字）

### 步骤 2：获取 API 密钥

1. 在微信支付商户平台，进入"账户中心" → "API安全"
2. 找到"API密钥"
3. 如果已设置，直接复制
4. 如果未设置或忘记，点击"设置"或"重置"

### 步骤 3：配置 application.yml

```yaml
wechat:
  pay:
    appid: wx1a44cbe925471245  # 你的小程序 AppID
    mchid: 1234567890  # 你的商户号（纯数字）
    apiKey: your-32-char-api-key  # 你的 API 密钥（32位）
    notifyUrl: https://your-domain.com/api/payment/notify  # 你的回调URL
```

### 步骤 4：验证配置

启动后端服务，查看日志：

```
微信支付配置初始化成功，AppID: wx1a44cbe925471245, MchID: 1234567890
```

如果看到错误信息，检查：
- 商户号格式是否正确（纯数字）
- API 密钥是否正确
- 配置文件中是否有拼写错误

## 常见错误

### 错误 1：mch_id参数格式错误

**原因：** 商户号配置成了 AppID

**解决：** 将 `mchid` 改为纯数字的商户号

### 错误 2：商户号不存在

**原因：** 商户号配置错误，或商户号未审核通过

**解决：** 
- 检查商户号是否正确
- 确认商户号已审核通过

### 错误 3：API密钥错误

**原因：** API 密钥配置错误

**解决：** 
- 检查 API 密钥是否正确
- 确认密钥已正确设置（不是 AppSecret）

## 开发环境配置

### 使用沙箱环境（推荐）

如果还没有真实商户号，可以使用沙箱环境：

1. 访问 [微信支付沙箱环境](https://pay.weixin.qq.com/wiki/doc/api/jsapi.php?chapter=23_1)
2. 获取沙箱商户号和密钥
3. 在 `application.yml` 中配置沙箱参数

```yaml
wechat:
  pay:
    appid: wx1a44cbe925471245  # 你的小程序 AppID
    mchid: 1234567890  # 沙箱商户号（纯数字）
    apiKey: sandbox-api-key  # 沙箱 API 密钥
    notifyUrl: https://your-domain.com/api/payment/notify
```

## 配置验证

代码已添加配置验证，启动时会检查：

1. ✅ AppID 是否配置
2. ✅ 商户号格式是否正确（8-10位纯数字）
3. ✅ API 密钥是否配置

如果配置错误，启动时会立即报错，便于快速定位问题。

## 总结

**关键点：**
- ✅ AppID：`wx` 开头的字符串
- ✅ 商户号：8-10位纯数字
- ✅ API 密钥：32位字符串
- ✅ 回调URL：必须是 HTTPS

**配置时注意：**
- 不要将 AppID 和商户号混淆
- 商户号必须是纯数字
- API 密钥不是 AppSecret

