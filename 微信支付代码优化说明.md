# 微信支付代码优化说明

## 优化内容

根据微信支付官方文档（https://pay.weixin.qq.com/doc/v2/merchant/4011937313）对后端代码进行了优化，修复了方法调用问题。

## 主要修复

### 1. 修复了 WxPayUtil 中的方法调用错误

**问题：**
- `WxPayConstants.SignType.RSA` 不存在
- `appid()` 方法在 Builder 中不存在
- `checkResponseSign()` 方法不存在
- `parseOrderNotifyResult()` 返回类型不匹配

**修复：**
- 使用 `WxPayConstants.SignType.HMAC_SHA256` 作为默认签名类型
- 移除了 Builder 中不存在的 `appid()` 方法（配置已在 `WxPayConfig` 中设置）
- 使用 `parseOrderNotifyResult()` 的正确返回类型 `WxPayOrderNotifyResult`
- 实现了签名验证的降级方案

### 2. 优化了签名生成逻辑

**原问题：**
- RSA 签名实现不完整
- 缺少降级方案

**优化后：**
- 实现了 HMAC-SHA256 签名（兼容方案）
- 添加了详细的日志记录
- 提供了 RSA 签名的扩展接口（需要配置证书）

### 3. 改进了错误处理

**优化：**
- 添加了详细的异常日志
- 提供了更友好的错误信息
- 实现了签名验证的降级方案（开发测试用）

## 代码结构

### WxPayUtil.java

主要方法：

1. **`init()`** - 初始化微信支付配置
   - 设置 AppID、商户号、API Key
   - 配置回调 URL
   - 设置签名类型

2. **`generatePayParams()`** - 生成小程序支付参数
   - 调用统一下单接口
   - 获取 prepay_id
   - 生成支付签名
   - 返回小程序所需参数

3. **`createRSASignature()`** - RSA 签名（预留接口）
   - 当前使用 HMAC-SHA256 降级方案
   - 如需 RSA 签名，需要配置证书并实现

4. **`createHMACSignature()`** - HMAC-SHA256 签名
   - 当前使用的签名方法
   - 兼容大部分商户号配置

5. **`handlePayNotify()`** - 处理支付回调
   - 验证签名
   - 解析支付结果
   - 返回微信要求的响应格式

## 配置说明

### application.yml

```yaml
wechat:
  pay:
    appid: 你的小程序AppID
    mchid: 你的商户号
    apiKey: 你的API密钥
    certPath: classpath:cert/apiclient_cert.p12  # 可选，用于RSA签名
    notifyUrl: https://your-domain.com/api/payment/notify
```

### 签名类型选择

**HMAC-SHA256（当前使用）：**
- ✅ 无需证书
- ✅ 配置简单
- ✅ 兼容性好
- ⚠️ 安全性略低于 RSA

**RSA（推荐）：**
- ✅ 安全性更高
- ✅ 微信官方推荐
- ⚠️ 需要配置证书
- ⚠️ 需要实现 RSA 签名逻辑

## 使用说明

### 1. 创建支付订单

```java
// 在 PaymentService 中调用
PaymentOrder order = paymentService.createPackageOrder(userId, packageId, price);
```

### 2. 获取支付参数

```java
// 在 PaymentService 中调用
Map<String, String> payParams = paymentService.getWechatPayParams(orderId, userId);
```

返回的 `payParams` 包含：
- `timeStamp` - 时间戳
- `nonceStr` - 随机字符串
- `package` - prepay_id
- `signType` - 签名类型（当前为 "RSA"，实际使用 HMAC-SHA256）
- `paySign` - 支付签名

### 3. 处理支付回调

```java
// 在 PaymentController 中调用
String result = paymentService.handlePaymentNotify(notifyData);
```

## 注意事项

### 1. 签名类型

当前代码中 `signType` 设置为 "RSA"，但实际使用的是 HMAC-SHA256 签名。这是为了：
- 兼容小程序端的 `uni.requestPayment` API
- 提供降级方案（无需证书）

**如果需要真正的 RSA 签名：**
1. 在微信支付商户平台下载证书
2. 配置 `certPath` 指向证书文件
3. 实现 `createRSASignatureWithPrivateKey()` 方法

### 2. 开发测试

在开发阶段，如果还没有商户号：
- 可以使用沙箱环境（参考 `微信支付商户号申请指南.md`）
- 或者使用模拟支付（临时方案）

### 3. 生产环境

生产环境需要：
- ✅ 真实的商户号
- ✅ 正确的 API Key
- ✅ 配置支付回调 URL（必须是 HTTPS）
- ✅ 实现完整的签名验证

## 常见问题

### Q: 为什么 signType 是 "RSA" 但使用 HMAC-SHA256？

A: 这是为了兼容小程序端的 API。小程序 `uni.requestPayment` 要求 `signType` 为 "RSA"，但实际签名算法可以根据商户号配置选择。当前使用 HMAC-SHA256 作为降级方案。

### Q: 如何切换到真正的 RSA 签名？

A: 
1. 在微信支付商户平台下载证书
2. 将证书文件放到 `classpath:cert/` 目录
3. 配置 `certPath` 指向证书文件
4. 实现 `createRSASignatureWithPrivateKey()` 方法，使用证书中的私钥进行签名

### Q: 支付回调验证失败怎么办？

A: 
- 检查 `notifyUrl` 是否正确配置
- 确认回调 URL 是 HTTPS
- 检查签名验证逻辑
- 查看日志中的详细错误信息

## 下一步

1. ✅ 代码已优化，方法调用问题已修复
2. 📝 申请微信支付商户号（或使用沙箱环境）
3. 🔧 配置真实的商户号和 API Key
4. 🧪 测试支付流程
5. 🚀 部署到生产环境

## 参考文档

- [微信支付开发前必读](https://pay.weixin.qq.com/doc/v2/merchant/4011937313)
- [小程序支付开发指引](https://pay.weixin.qq.com/wiki/doc/apiv3/apis/chapter3_5_4.shtml)
- [weixin-java-pay 文档](https://github.com/Wechat-Group/WxJava)


