# 城市森林氧舱小程序需求文档

## 项目概述

**项目名称：** 城市森林氧舱  
**技术栈：** uni-app (Vue 3 + Vite)  
**项目类型：** 微信小程序  
**主要功能：** 氧舱体验预约、会员商城、订单管理

---

## 一、页面结构

### 1.1 页面路由

小程序共包含4个主要页面：

1. **首页** (`pages/index/index`)
   - 路径：`/pages/index/index`
   - 标题：城市森林氧舱
   - 导航样式：自定义导航栏

2. **会员商城** (`pages/store/store`)
   - 路径：`/pages/store/store`
   - 标题：会员商城
   - 导航样式：自定义导航栏

3. **立即预约** (`pages/booking/booking`)
   - 路径：`/pages/booking/booking`
   - 标题：立即预约
   - 导航样式：自定义导航栏

4. **资产中心** (`pages/my/my`)
   - 路径：`/pages/my/my`
   - 标题：资产中心
   - 导航样式：自定义导航栏

### 1.2 底部导航

**组件位置：** `components/BottomNav.vue`

**导航项：**
- 首页（图标：home.png / home-active.png）
- 会员购（图标：store.png / store-active.png）
- 预约（图标：booking.png / booking-active.png）
- 我的（图标：profile.png / profile-active.png）

**导航逻辑：**
- 首页、会员购、我的：使用 `uni.reLaunch` 切换（主要页面）
- 预约：使用 `uni.navigateTo` 跳转（子页面）

---

## 二、首页功能需求

### 2.1 Hero Banner 区域

**功能描述：** 展示品牌主视觉和核心信息

**包含元素：**
1. **背景图片**
   - 显示氧舱环境图片
   - 支持图片填充模式（aspectFill）

2. **标签**
   - "PREMIUM WELLNESS" 标签
   - "O₂ ION" 标签
   - 样式：半透明背景，白色边框，圆角

3. **主标题**
   - 第一行："城市"
   - 第二行："森林氧舱"
   - 样式：大号白色字体

4. **描述文字**
   - "在喧嚣都市中,为您找回"
   - "每一次深呼吸的纯净与宁静。"
   - 样式：中等大小白色字体，左侧边框装饰

**布局要求：**
- 高度：1000rpx
- 底部圆角：80rpx
- 内容区域上内边距：200rpx（避免被状态栏遮挡）

### 2.2 VIP 会员卡片

**功能描述：** 展示会员权益，引导用户开通会员

**位置：** 覆盖在 Hero Banner 底部，重叠约一半

**包含内容：**
1. **左侧信息**
   - 图标：✦（金色星标）
   - 标题："成为尊享会员"
   - 价格信息：
     - "单次体验低至"
     - "¥58"（突出显示）
     - "/ 次"

2. **右侧按钮**
   - 圆形按钮，带箭头图标
   - 点击可跳转到会员商城

**样式要求：**
- 深色渐变背景（#2c2a26 到 #1a1917）
- 圆角：48rpx
- 内边距：60rpx（上）50rpx（左右）40rpx（下）
- 阴影效果

### 2.3 单次体验卡片

**功能描述：** 展示单次体验服务，支持立即预约

**包含内容：**
1. **头部信息**
   - 标题："深度单次体验"
   - 描述："60分钟纯净富氧环境，包含茶歇"
   - 价格：
     - 现价：¥398
     - 原价：¥598（删除线）

2. **功能特性**
   - 60min（时长）
   - 私密空间（环境特点）
   - 带图标展示

3. **操作按钮**
   - "立即预约"按钮
   - 点击跳转到预约页面

**样式要求：**
- 白色背景
- 圆角：48rpx
- 内边距：58rpx（上下左右一致）
- 阴影效果

### 2.4 环境与服务画廊

**功能描述：** 横向滚动展示服务环境图片

**包含内容：**
- 标题："环境与服务"
- 副标题："Gallery"
- 横向滚动图片列表
  - 独立座舱（Private Cabin）
  - 茶歇服务（Tea Service）
  - 纯净负离子（Fresh Air）

**交互要求：**
- 支持横向滚动
- 隐藏滚动条
- 每张图片显示中英文名称

---

## 三、会员商城功能需求

### 3.1 页面头部

**包含内容：**
- 标题："会员商城"
- 副标题："定制您的专属健康方案"

### 3.2 分类标签

**功能描述：** 切换不同会员套餐类型

**标签项：**
1. **个人畅享**（默认选中）
2. **多人尊享**
3. **家庭/次卡**

**交互要求：**
- 点击切换标签
- 选中状态有视觉反馈
- 切换标签时更新下方套餐列表

### 3.3 会员套餐列表

**功能描述：** 展示各类会员套餐卡片

**套餐信息包含：**
1. **标签徽章**（可选）
   - "新人推荐"
   - "超值"
   - 其他营销标签

2. **套餐标题**
   - 如："个人月卡"、"个人年卡"等

3. **套餐描述**
   - 如："尊享30天无限次纯净体验"

4. **价格信息**
   - 现价（突出显示）
   - 原价（删除线）
   - 单次价格（可选，如："约 ¥56/次"）

5. **功能特性列表**
   - 有效期（如：30天、365天）
   - 使用次数（如：无限次）
   - 绑定人数（如：支持1人绑定）
   - 其他权益

6. **操作按钮**
   - "立即开通"按钮
   - 带箭头图标

**套餐数据示例：**
- 个人月卡：¥1680（原价¥2980），约¥56/次，30天无限次
- 个人年卡：¥9800（原价¥15800），约¥27/次，365天无限次
- 个人季卡：¥3680（原价¥5800），90天无限次
- 个人半年卡：¥5800（原价¥9800），180天无限次

**交互要求：**
- 支持纵向滚动
- 卡片点击可查看详情或直接购买

---

## 四、预约功能需求

### 4.1 页面头部

**包含内容：**
- 标题："立即预约"

### 4.2 日期选择

**功能描述：** 选择预约日期

**实现方式：**
- 横向滚动日期列表
- 自动生成未来15天的日期选项
- 日期格式：MM.DD（如：01.15）
- 显示星期几（如：周一、周二）

**日期生成规则：**
- 从当前日期开始
- 生成未来15天
- 自动计算对应的星期几
- 格式：`{ number: '01.15', weekday: '周一' }`

**交互要求：**
- 支持横向滚动
- 点击选择日期
- 选中状态：深绿色背景，白色文字
- 未选中状态：白色背景，灰色文字

### 4.3 时段选择

**功能描述：** 选择预约时间段

**时段列表：**
- 10:00–11:00
- 11:15–12:15
- 12:30–13:30
- 13:45–14:45
- 15:00–16:00
- 16:15–17:15
- 17:30–18:30
- 18:45–19:45
- 20:00–21:00

**布局要求：**
- 3列网格布局
- 每个时段一个卡片

**交互要求：**
- 点击选择时段
- 选中状态：浅绿色背景，深绿色边框，右上角显示✓
- 再次点击可取消选择

### 4.4 舱位选择

**功能描述：** 选择具体的舱位和座位

**舱位结构：**
- 1号舱：A座、B座
- 2号舱：A座、B座

**布局要求：**
- 2列网格布局
- 每个舱位一个卡片
- 舱位名称居中显示
- 座位横向排列

**交互要求：**
- 点击座位选择
- 选中状态：深绿色背景，白色文字
- 未选中状态：白色背景，黑色文字

### 4.5 预约摘要栏

**功能描述：** 显示预约信息摘要和支付按钮

**位置：** 固定在页面底部（底部导航上方）

**包含内容：**
1. **预约信息**
   - 未完成时显示："请选择时间和座位"
   - 完成时显示：日期 时段 舱位-座位（如：01月15日 10:00–11:00 1号舱-A座）

2. **价格信息**
   - 标签："总计:"
   - 现价：¥398（突出显示）
   - 原价：¥598（删除线）

3. **支付按钮**
   - 文字："支付并预约"
   - 未完成选择时：禁用状态（半透明）
   - 完成选择时：可点击状态

**交互要求：**
- 只有完成日期、时段、座位选择后，按钮才可点击
- 点击按钮触发支付流程

---

## 五、资产中心功能需求

### 5.1 页面头部

**包含内容：**
- 标题："资产中心"
- 设置图标（右上角）

### 5.2 用户信息区域

**功能描述：** 展示当前登录用户的基本信息

**包含内容：**
1. **用户头像**
   - 圆形头像
   - 支持图片填充

2. **用户信息**
   - 用户名："微信用户"（默认）
   - 手机号："138****8888"（脱敏显示）

### 5.3 会员卡片

**功能描述：** 展示用户会员状态和权益

**包含内容：**
1. **会员头部**
   - 皇冠图标
   - 会员等级："普通用户"（默认）
   - 状态描述："尚未开通会员"

2. **会员统计**
   - 剩余次数：显示 "-"（未开通时）
   - 已绑定：显示 "0"
   - 积分余额：显示 "0"

**样式要求：**
- 白色背景
- 圆角卡片
- 统计信息横向排列

### 5.4 预约订单列表

**功能描述：** 展示用户的预约订单记录

**包含内容：**
1. **订单标签**
   - "全部"（默认选中）
   - "待核销"
   - "已完成"

2. **订单列表项**
   - 订单头部：
     - 舱位标签（如："1号舱"）
     - 预约日期（如："2025-01-02"）
     - 订单状态（如："已完成"）
   - 订单详情：
     - 时段信息（带时钟图标）："10:00–11:00"
     - 座位信息（带座位图标）："A座"

**交互要求：**
- 点击标签切换订单状态筛选
- 支持纵向滚动查看所有订单
- 订单项可点击查看详情

---

## 六、数据模型

### 6.1 日期数据模型

```javascript
{
  number: '01.15',    // 日期格式：MM.DD
  weekday: '周一'      // 星期几（中文）
}
```

**生成规则：**
- 从当前日期开始
- 自动生成未来15天
- 自动计算星期几

### 6.2 会员套餐数据模型

```javascript
{
  badge: '新人推荐',           // 标签（可选）
  title: '个人月卡',           // 套餐名称
  desc: '尊享30天无限次纯净体验', // 套餐描述
  price: '¥1680',             // 现价
  originalPrice: '¥2980',     // 原价
  perTime: '约 ¥56/次',       // 单次价格（可选）
  features: [                 // 功能特性列表
    '有效期30天',
    '无限次',
    '支持1人绑定'
  ]
}
```

### 6.3 预约订单数据模型

```javascript
{
  cabin: '1号舱',           // 舱位
  date: '2025-01-02',      // 预约日期
  status: '已完成',         // 订单状态
  time: '10:00–11:00',     // 时段
  seat: 'A座'              // 座位
}
```

### 6.4 时段数据模型

```javascript
[
  '10:00–11:00',
  '11:15–12:15',
  '12:30–13:30',
  '13:45–14:45',
  '15:00–16:00',
  '16:15–17:15',
  '17:30–18:30',
  '18:45–19:45',
  '20:00–21:00'
]
```

### 6.5 舱位数据模型

```javascript
[
  {
    name: '1号舱',
    seats: ['A座', 'B座']
  },
  {
    name: '2号舱',
    seats: ['A座', 'B座']
  }
]
```

---

## 七、交互逻辑

### 7.1 页面导航

**主要页面切换：**
- 首页 ↔ 会员商城 ↔ 资产中心
- 使用 `uni.reLaunch` 实现
- 切换时重置页面状态

**子页面跳转：**
- 首页 → 预约页面：使用 `uni.navigateTo`
- 预约页面可返回首页
- 使用 `uni.navigateBack` 返回

### 7.2 预约流程

**步骤：**
1. 选择日期（必选）
2. 选择时段（必选）
3. 选择舱位和座位（必选）
4. 确认预约信息
5. 支付并预约

**状态管理：**
- 使用 `selectedDate`、`selectedTime`、`selectedSeat` 记录选择状态
- 只有全部选择完成后，支付按钮才可点击
- 使用计算属性 `isComplete` 判断是否完成

### 7.3 会员套餐切换

**交互：**
- 点击标签切换套餐类型
- 切换时更新套餐列表
- 保持滚动位置（可选）

---

## 八、样式规范

### 8.1 颜色规范

**主色调：**
- 深绿色：`#4a5d50`（主要按钮、选中状态）
- 浅绿色：`rgba(74, 93, 80, 0.05)`（选中背景）

**文字颜色：**
- 主文字：`#333333`
- 次要文字：`#99a1af`
- 白色文字：`#ffffff`（深色背景上）

**背景颜色：**
- 页面背景：`#f9f9f9`
- 卡片背景：`#ffffff`
- VIP卡片：渐变 `#2c2a26` → `#1a1917`

### 8.2 字体规范

**标题：**
- 大标题：40rpx，字重 500
- 中标题：36rpx，字重 500
- 小标题：32rpx，字重 500

**正文：**
- 主要文字：28rpx，字重 300
- 次要文字：24rpx，字重 300
- 辅助文字：20rpx，字重 300

### 8.3 间距规范

**页面内边距：**
- 主要内容区：48rpx
- 卡片内边距：50-58rpx

**元素间距：**
- 大间距：64rpx
- 中间距：32-48rpx
- 小间距：16-24rpx

### 8.4 圆角规范

- 大圆角：48rpx（主要卡片）
- 中圆角：32rpx（次要卡片）
- 小圆角：20-28rpx（按钮、标签）

---

## 九、技术实现要点

### 9.1 日期生成

**实现方式：**
- 使用 JavaScript `Date` 对象
- 循环生成未来15天
- 使用 `getDay()` 获取星期几
- 格式化日期为 MM.DD 格式

**关键代码：**
```javascript
generateDates() {
  const dates = []
  const weekdays = ['周日', '周一', '周二', '周三', '周四', '周五', '周六']
  const today = new Date()
  
  for (let i = 0; i < 15; i++) {
    const date = new Date(today)
    date.setDate(today.getDate() + i)
    
    const month = String(date.getMonth() + 1).padStart(2, '0')
    const day = String(date.getDate()).padStart(2, '0')
    const weekday = weekdays[date.getDay()]
    
    dates.push({
      number: `${month}.${day}`,
      weekday: weekday
    })
  }
  
  return dates
}
```

### 9.2 状态管理

**预约状态：**
- 使用组件内部 `data` 管理
- 通过计算属性判断完成状态
- 支持单选和多选逻辑

### 9.3 图片资源

**静态资源路径：**
- 图标：`/static/` 目录
- 背景图片：使用 Figma CDN 或本地资源
- 支持 mode 属性控制图片显示方式

---

## 十、待实现功能

### 10.1 后端接口对接

**需要对接的接口：**
1. 用户登录/授权
2. 会员套餐列表（支持分类筛选）
3. 提交预约订单
4. 支付接口
5. 订单列表查询
6. 用户信息获取/更新

### 10.2 功能增强

**待完善功能：**
1. 会员开通流程
2. 支付流程
3. 订单详情页
4. 订单取消/退款
5. 会员权益详情
6. 设置页面
7. 消息通知

### 10.3 优化建议

**性能优化：**
1. 图片懒加载
2. 列表虚拟滚动（如果数据量大）
3. 接口请求缓存

**用户体验：**
1. 加载状态提示
2. 错误提示和重试
3. 空状态展示
4. 下拉刷新

---

## 十一、项目文件结构

```
my0110-6/
├── pages/
│   ├── index/
│   │   └── index.vue          # 首页
│   ├── store/
│   │   └── store.vue          # 会员商城
│   ├── booking/
│   │   └── booking.vue        # 立即预约
│   └── my/
│       └── my.vue             # 资产中心
├── components/
│   └── BottomNav.vue          # 底部导航组件
├── static/
│   ├── home.png               # 首页图标
│   ├── home-active.png        # 首页激活图标
│   ├── store.png              # 会员购图标
│   ├── store-active.png       # 会员购激活图标
│   ├── booking.png            # 预约图标
│   ├── booking-active.png     # 预约激活图标
│   ├── profile.png            # 我的图标
│   └── profile-active.png     # 我的激活图标
├── pages.json                 # 页面配置
├── vite.config.js            # Vite 配置
└── package.json              # 项目依赖
```

---

## 十二、总结

### 12.1 核心功能

1. **首页展示**：品牌展示、服务介绍、会员推广
2. **会员商城**：套餐展示、分类筛选、会员开通
3. **预约服务**：日期选择、时段选择、舱位选择、订单提交
4. **资产中心**：用户信息、会员状态、订单管理

### 12.2 技术特点

- 使用 uni-app 框架，支持多端发布
- Vue 3 组合式 API
- 自定义导航栏和底部导航
- 响应式布局设计
- 组件化开发

### 12.3 设计亮点

- 现代化的 UI 设计
- 流畅的交互体验
- 清晰的视觉层次
- 统一的色彩规范

---

**文档版本：** v1.0  
**最后更新：** 2025-01-11  
**维护者：** 开发团队

---

