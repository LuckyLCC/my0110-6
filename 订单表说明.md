# 订单表说明

## 两个订单表的区别

### 1. `payment_orders` - 支付订单表

**用途：** 记录用户购买会员套餐的支付订单

**业务场景：** 用户在"会员商城"页面购买会员套餐（月卡、季卡、年卡等）

**创建时机：** 
- 用户在"会员商城"页面点击"立即开通"
- 跳转到"支付确认"页面
- 点击"立即支付"后创建

**表结构：**
- `id` - 订单ID
- `order_no` - 订单号（格式：PAY + 时间戳）
- `user_id` - 用户ID
- `package_id` - 套餐ID
- `package_name` - 套餐名称
- `price` - 价格
- `status` - 订单状态（unpaid, paid, cancelled）
- `payment_time` - 支付时间
- `created_at` - 创建时间

**示例数据：**
```sql
SELECT * FROM payment_orders;
-- 用户购买了"月卡"，价格3980元
-- 订单状态：paid（已支付）
```

### 2. `booking_orders` - 预约订单表

**用途：** 记录用户预约使用氧舱服务的订单

**业务场景：** 用户在"预约"页面选择日期、时间段、舱位后创建预约

**创建时机：**
- 用户在"预约"页面选择日期
- 选择时间段（如：10:00-11:00）
- 选择舱位（如：1号舱-A座）
- 点击"支付并预约"后创建

**表结构：**
- `id` - 订单ID
- `order_no` - 订单号（格式：BK + 时间戳）
- `user_id` - 用户ID
- `date` - 预约日期（格式：YYYY-MM-DD）
- `time_slot` - 时间段（格式：HH:mm-HH:mm）
- `cabin_name` - 舱室名称（如：1号舱）
- `seat_name` - 座位名称（如：A座）
- `price` - 价格
- `status` - 订单状态（pending, completed）
- `payment_status` - 支付状态（unpaid, paid）
- `created_at` - 创建时间

**示例数据：**
```sql
SELECT * FROM booking_orders;
-- 用户预约了"2024-01-15"的"10:00-11:00"时间段
-- 舱位：1号舱-A座
-- 订单状态：pending（待核销）
```

## 业务流程

### 流程1：购买会员套餐

```
用户 → 会员商城页面 → 选择套餐 → 支付确认页面 → 支付 → payment_orders表
```

**结果：** 
- ✅ `payment_orders` 表有数据
- ❌ `booking_orders` 表没有数据（这是正常的）

### 流程2：预约服务

```
用户 → 预约页面 → 选择日期/时间/舱位 → 支付并预约 → booking_orders表
```

**结果：**
- ✅ `booking_orders` 表有数据

## 为什么 booking_orders 没有数据？

**原因：** 这是正常的！因为：

1. **两个独立的业务流程**
   - 购买会员套餐 ≠ 预约服务
   - 购买会员套餐只是获得会员权益
   - 预约服务是使用会员权益的具体操作

2. **需要用户主动操作**
   - 购买会员套餐后，用户还需要：
     - 进入"预约"页面
     - 选择日期、时间、舱位
     - 点击"支付并预约"
   - 只有完成这些操作，才会创建 `booking_orders` 记录

## 如何创建 booking_orders 数据？

### 方法1：通过小程序界面（推荐）

1. 打开小程序
2. 进入"预约"页面（底部导航栏）
3. 选择日期（如：明天）
4. 选择时间段（如：10:00-11:00）
5. 选择舱位（如：1号舱-A座）
6. 点击"支付并预约"
7. 完成支付（或使用Mock模式模拟支付成功）

### 方法2：通过API直接创建（测试用）

```bash
POST /api/booking/create
Authorization: Bearer {token}
Content-Type: application/x-www-form-urlencoded

date=2024-01-15
timeSlot=10:00-11:00
cabinName=1号舱
seatName=A座
price=398.0
originalPrice=598.0
```

### 方法3：通过数据库直接插入（不推荐）

```sql
INSERT INTO booking_orders (
    order_no, user_id, date, time_slot, cabin_name, seat_name, 
    price, original_price, status, payment_status, created_at, updated_at
) VALUES (
    'BK' || UNIX_TIMESTAMP() || LPAD(FLOOR(RAND() * 10000), 4, '0'),
    1,  -- 替换为实际用户ID
    '2024-01-15',
    '10:00-11:00',
    '1号舱',
    'A座',
    398.0,
    598.0,
    'pending',
    'unpaid',
    NOW(),
    NOW()
);
```

## 数据关系

### 用户购买会员套餐后

```sql
-- 查看用户的支付订单
SELECT * FROM payment_orders WHERE user_id = 1;

-- 结果：有数据（用户购买了会员套餐）
-- status: paid（已支付）
```

### 用户预约服务后

```sql
-- 查看用户的预约订单
SELECT * FROM booking_orders WHERE user_id = 1;

-- 结果：有数据（用户预约了服务）
-- status: pending（待核销）
-- payment_status: paid（已支付）
```

## 常见问题

### Q: 为什么我支付了会员套餐，但 booking_orders 表还是空的？

A: 这是正常的！购买会员套餐和预约服务是两个独立的操作：
- 购买会员套餐 → 获得会员权益 → `payment_orders` 有数据
- 预约服务 → 使用会员权益 → `booking_orders` 有数据

你需要：
1. 进入"预约"页面
2. 选择日期、时间、舱位
3. 点击"支付并预约"
4. 这样才会创建 `booking_orders` 记录

### Q: 购买会员套餐后，还需要支付预约费用吗？

A: 这取决于业务逻辑：
- 如果会员套餐包含免费预约次数 → 预约时不需要支付
- 如果会员套餐只是折扣 → 预约时仍需要支付（享受折扣）

当前代码中，预约时仍需要支付，但价格可能享受会员折扣。

### Q: 如何查看用户的完整订单记录？

A: 需要分别查询两个表：

```sql
-- 查看支付订单（会员套餐购买）
SELECT * FROM payment_orders WHERE user_id = 1;

-- 查看预约订单（服务预约）
SELECT * FROM booking_orders WHERE user_id = 1;
```

## 总结

- ✅ `payment_orders` 有数据 = 用户购买了会员套餐
- ✅ `booking_orders` 有数据 = 用户预约了服务
- ⚠️ 两个表是独立的，购买会员套餐不会自动创建预约订单
- 📝 要创建 `booking_orders` 数据，需要在"预约"页面完成预约操作


