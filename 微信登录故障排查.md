# 微信登录故障排查指南

## 错误："微信登录失败: null"

这个错误通常表示后端在调用微信 API 时出现了问题，但异常信息没有正确传递。

## 已修复的问题

我已经修复了 `WechatUtil.java` 中的以下问题：

1. ✅ **添加了配置检查**：在调用微信 API 前检查 AppID 和 AppSecret 是否配置
2. ✅ **改进了错误处理**：更详细地检查微信 API 的响应
3. ✅ **添加了错误码说明**：常见错误码的详细说明
4. ✅ **防止空指针异常**：在访问 JSON 字段前先检查是否存在

## 常见原因及解决方案

### 1. AppID 或 AppSecret 配置错误

**检查方法：**
- 查看 `backend/src/main/resources/application.yml` 文件
- 确认 `wechat.miniapp.appid` 和 `wechat.miniapp.secret` 已正确配置

**解决方案：**
```yaml
wechat:
  miniapp:
    appid: wx你的AppID  # 确保没有多余空格
    secret: 你的AppSecret  # 确保没有多余空格
```

**验证：**
- 登录微信公众平台：https://mp.weixin.qq.com/
- 开发 → 开发管理 → 开发设置
- 对比 AppID 和 AppSecret 是否一致

### 2. Code 无效或已过期

**原因：**
- Code 只能使用一次
- Code 有效期只有 5 分钟
- 如果重复使用或超时，会返回错误

**解决方案：**
- 每次登录都重新调用 `uni.login` 获取新的 code
- 确保 code 在 5 分钟内使用

**检查方法：**
在前端控制台查看：
```javascript
// 查看获取到的 code
console.log('微信 code:', res.code)
```

### 3. 网络问题

**可能原因：**
- 后端服务器无法访问微信 API（`https://api.weixin.qq.com`）
- 防火墙阻止了请求

**解决方案：**
- 检查服务器网络连接
- 确认可以访问 `https://api.weixin.qq.com`
- 检查防火墙设置

**测试方法：**
```bash
# 在服务器上测试
curl "https://api.weixin.qq.com/sns/jscode2session?appid=你的AppID&secret=你的Secret&js_code=测试code&grant_type=authorization_code"
```

### 4. 微信 API 返回错误

**常见错误码：**

| 错误码 | 说明 | 解决方案 |
|--------|------|----------|
| 40013 | AppID 无效 | 检查 AppID 是否正确 |
| 40125 | AppSecret 无效 | 检查 AppSecret 是否正确，可能需要重置 |
| 40029 | code 无效或已过期 | 重新获取 code |
| 45011 | API 调用太频繁 | 等待一段时间后重试 |
| -1 | 系统繁忙 | 稍后重试 |

**查看详细错误：**
修复后的代码会显示详细的错误信息，包括错误码和说明。

### 5. 后端日志查看

**查看后端日志：**
```bash
# 进入后端目录
cd backend

# 查看日志文件
tail -f logs/application.log

# 或者查看最新的错误日志
grep "微信登录" logs/application.log | tail -20
```

**日志中应该能看到：**
- 微信 API 的完整响应
- 具体的错误信息
- 异常堆栈信息

## 调试步骤

### 步骤 1：检查配置

1. 打开 `backend/src/main/resources/application.yml`
2. 确认 `wechat.miniapp.appid` 和 `wechat.miniapp.secret` 已配置
3. 重启后端服务

### 步骤 2：检查前端日志

1. 打开微信开发者工具
2. 打开控制台
3. 点击"微信一键登录"
4. 查看控制台输出：
   ```javascript
   // 应该能看到
   uni.login 返回: { code: "xxx" }
   后端登录响应: { code: 200, ... }
   ```

### 步骤 3：检查后端日志

1. 查看后端日志文件
2. 查找包含 "微信登录" 的日志
3. 查看具体的错误信息

### 步骤 4：测试微信 API

如果配置正确，可以手动测试微信 API：

```bash
# 替换为你的实际值
APPID="wx你的AppID"
SECRET="你的AppSecret"
CODE="从前端获取的code"

curl "https://api.weixin.qq.com/sns/jscode2session?appid=${APPID}&secret=${SECRET}&js_code=${CODE}&grant_type=authorization_code"
```

**正常响应示例：**
```json
{
  "openid": "oUpF8uMuAJO_M2pxb1Q9zNjWeS6o",
  "session_key": "tiihtNczf5v6AKRyjwEUhQ=="
}
```

**错误响应示例：**
```json
{
  "errcode": 40029,
  "errmsg": "invalid code"
}
```

## 修复后的代码改进

修复后的 `WechatUtil.java` 会：

1. ✅ **检查配置**：在调用前验证 AppID 和 AppSecret
2. ✅ **详细错误信息**：显示具体的错误码和说明
3. ✅ **防止空指针**：在访问 JSON 字段前先检查
4. ✅ **更好的异常处理**：确保错误信息能正确传递

## 如果问题仍然存在

1. **查看完整日志**：
   ```bash
   tail -100 backend/logs/application.log
   ```

2. **检查微信公众平台**：
   - 确认 AppID 和 AppSecret 正确
   - 确认小程序状态正常
   - 检查是否有违规记录

3. **测试网络连接**：
   ```bash
   ping api.weixin.qq.com
   curl -I https://api.weixin.qq.com
   ```

4. **重新生成 AppSecret**：
   - 如果怀疑 AppSecret 泄露或错误
   - 在微信公众平台重置 AppSecret
   - 更新配置文件并重启服务

## 联系支持

如果以上方法都无法解决问题，请提供以下信息：

1. 后端日志（包含错误堆栈）
2. 前端控制台日志
3. 微信 API 的原始响应（如果有）
4. AppID（可以公开，但不要提供 AppSecret）

