# 微信支付商户号申请指南

## 申请微信支付商户号

### 步骤 1：访问微信支付商户平台

1. 访问 [微信支付商户平台](https://pay.weixin.qq.com/)
2. 使用微信扫码登录（需要使用小程序管理员的微信）

### 步骤 2：注册商户号

1. 点击"注册"或"成为商户"
2. 选择注册类型：
   - **企业**：需要营业执照
   - **个体工商户**：需要营业执照
   - **政府及事业单位**：需要组织机构代码证
   - **其他组织**：需要相关资质证明

### 步骤 3：填写资料

需要准备的材料：
- 营业执照（企业/个体户）
- 法人身份证
- 对公账户信息
- 联系人信息
- 经营类目

### 步骤 4：等待审核

- 审核时间：通常 1-7 个工作日
- 审核通过后，会收到微信支付商户号

### 步骤 5：配置支付参数

审核通过后：
1. 登录微信支付商户平台
2. 获取商户号（mch_id）
3. 设置 API 密钥
4. 配置支付回调 URL

## 开发阶段的替代方案

### 方案一：使用微信支付沙箱环境（推荐）

微信支付提供沙箱环境用于开发测试，**无需申请商户号**。

**特点：**
- ✅ 免费使用
- ✅ 无需真实商户号
- ✅ 可以测试完整支付流程
- ⚠️ 仅用于开发测试，不能真实收款

**使用步骤：**
1. 访问 [微信支付沙箱环境](https://pay.weixin.qq.com/wiki/doc/api/jsapi.php?chapter=23_1)
2. 使用小程序 AppID 登录
3. 获取沙箱环境的商户号和密钥
4. 在后端配置中使用沙箱参数

### 方案二：模拟支付（临时方案）

在开发阶段，可以暂时使用模拟支付，跳过真实的微信支付调用。

**实现方式：**
- 后端直接返回模拟的支付参数
- 前端调用 `uni.requestPayment` 时会失败（因为没有真实商户号）
- 可以添加一个"模拟支付成功"的按钮用于测试

### 方案三：延迟支付功能

如果暂时不需要支付功能，可以：
- 先完成其他功能开发
- 支付按钮显示"敬请期待"或"功能开发中"
- 等商户号申请完成后再接入真实支付

## 推荐方案：使用沙箱环境

### 1. 获取沙箱参数

访问：https://pay.weixin.qq.com/wiki/doc/api/jsapi.php?chapter=23_1

按照文档获取：
- 沙箱商户号（sandbox_mch_id）
- 沙箱密钥（sandbox_key）

### 2. 后端配置

在 `application.yml` 中配置沙箱参数：

```yaml
wechat:
  pay:
    appid: 你的小程序AppID
    mchid: 沙箱商户号
    apiKey: 沙箱密钥
    sandbox: true  # 标识使用沙箱环境
    notifyUrl: https://your-domain.com/api/payment/notify
```

### 3. 后端实现

在调用微信支付接口时，使用沙箱环境的 URL：
- 沙箱统一下单接口：`https://api.mch.weixin.qq.com/sandboxnew/pay/unifiedorder`
- 正式环境接口：`https://api.mch.weixin.qq.com/pay/unifiedorder`

## 临时测试方案（模拟支付）

如果暂时无法使用沙箱环境，可以修改后端接口，返回模拟的支付参数：

### 后端临时实现

```java
@PostMapping("/wechat-pay/{orderId}")
public Result<Map<String, String>> getWechatPayParams(
        @PathVariable Long orderId) {
    
    // 临时：返回模拟支付参数（仅用于开发测试）
    Map<String, String> payParams = new HashMap<>();
    payParams.put("timeStamp", String.valueOf(System.currentTimeMillis() / 1000));
    payParams.put("nonceStr", "TEST" + System.currentTimeMillis());
    payParams.put("package", "prepay_id=TEST_PREPAY_ID");
    payParams.put("signType", "RSA");
    payParams.put("paySign", "TEST_SIGN");
    
    return Result.success("获取支付参数成功（测试模式）", payParams);
}
```

### 前端临时处理

在前端添加测试模式，如果支付失败，可以手动触发"支付成功"：

```javascript
// 在支付失败时，添加测试按钮
fail: (err) => {
    // 开发环境：显示测试按钮
    if (process.env.NODE_ENV === 'development') {
        uni.showModal({
            title: '支付失败（测试模式）',
            content: '是否模拟支付成功？',
            success: (res) => {
                if (res.confirm) {
                    // 模拟支付成功
                    this.handlePaymentSuccess()
                }
            }
        })
    }
}
```

## 申请商户号的注意事项

### 1. 资质要求

- **企业**：需要营业执照、对公账户
- **个体工商户**：需要营业执照、法人银行卡
- **个人**：无法申请（需要企业或个体户）

### 2. 费用

- 申请费用：免费
- 交易手续费：按交易金额收取（通常 0.6%）
- 提现费用：免费（T+1 到账）

### 3. 审核时间

- 通常 1-7 个工作日
- 资料齐全可加快审核速度

### 4. 常见问题

**Q: 个人可以申请吗？**
A: 不可以，需要企业或个体工商户资质。

**Q: 没有营业执照怎么办？**
A: 可以注册个体工商户，申请营业执照相对简单。

**Q: 审核被拒怎么办？**
A: 查看拒绝原因，补充或修改资料后重新提交。

**Q: 可以先用测试账号开发吗？**
A: 可以，使用沙箱环境或模拟支付进行开发。

## 开发建议

### 阶段一：开发阶段（无商户号）

1. ✅ 使用沙箱环境或模拟支付
2. ✅ 完成支付流程的前端开发
3. ✅ 完成后端接口开发（使用沙箱参数）
4. ✅ 测试支付流程

### 阶段二：申请商户号

1. 📝 准备申请材料
2. 📝 提交申请
3. ⏳ 等待审核

### 阶段三：接入真实支付

1. 🔄 商户号审核通过后
2. 🔄 更新后端配置（使用真实商户号）
3. 🔄 测试真实支付流程
4. ✅ 上线

## 快速开始（使用沙箱）

1. **访问沙箱文档**：https://pay.weixin.qq.com/wiki/doc/api/jsapi.php?chapter=23_1
2. **获取沙箱参数**：按照文档获取商户号和密钥
3. **配置后端**：使用沙箱参数
4. **开始开发**：可以正常测试支付流程

## 总结

- ✅ **推荐**：使用微信支付沙箱环境（免费，无需申请商户号）
- ⚠️ **临时**：使用模拟支付（仅用于功能开发）
- 📝 **正式**：申请真实商户号（用于正式上线）

建议先使用沙箱环境完成开发，同时准备材料申请真实商户号。

