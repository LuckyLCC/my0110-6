# H5 启动详细步骤-1111

## 方式一：使用 HBuilderX IDE（最简单推荐）⭐⭐⭐

### 步骤详解：

1. **下载 HBuilderX**
   - 访问：https://www.dcloud.io/hbuilderx.html
   - 下载 **标准版** 或 **App开发版**
   - 解压即可使用（无需安装）

2. **打开项目**
   ```
   HBuilderX → 文件 → 打开目录 → 选择：/Users/liuchang/Downloads/my0110-6
   ```

3. **运行项目**
   - 点击顶部菜单 **运行(R)** → **运行到浏览器**
   - 选择浏览器：
     - Chrome（推荐）
     - Safari
     - Firefox
   - 浏览器会自动打开，显示页面

4. **查看效果**
   - 浏览器会自动打开 `http://localhost:8080` 或类似地址
   - 可以看到完整的页面效果
   - 修改代码后，按 `Ctrl+S` 保存，浏览器会自动刷新

---

## 方式二：使用命令行（需要 Node.js）⭐

### 前提条件：
- ✅ 已安装 Node.js（你已安装 v24.12.0）
- ✅ 已安装 npm（你已安装 v11.6.2）

### 步骤：

#### 1️⃣ 安装依赖

在项目目录执行：

```bash
cd /Users/liuchang/Downloads/my0110-6
npm install
```

这会安装所需的依赖包。

#### 2️⃣ 启动开发服务器

```bash
npm run dev:h5
```

或者直接使用 vite：

```bash
npx vite
```

#### 3️⃣ 访问页面

- 终端会显示类似信息：
  ```
  ➜  Local:   http://localhost:3000/
  ➜  Network: http://192.168.x.x:3000/
  ```
- 浏览器会自动打开，如果没有，手动访问 `http://localhost:3000`

#### 4️⃣ 停止服务器

在终端按 `Ctrl + C` 停止开发服务器

---

## 如果遇到问题

### 问题1：npm install 报错
```bash
# 清除缓存重试
npm cache clean --force
npm install
```

### 问题2：端口被占用
修改 `vite.config.js` 中的端口号：
```javascript
server: {
  port: 3001,  // 改为其他端口
  open: true
}
```

### 问题3：依赖安装失败
```bash
# 使用淘宝镜像
npm install --registry=https://registry.npmmirror.com
```

---

## 推荐方式

**强烈推荐使用 HBuilderX**，因为：
- ✅ 无需配置，开箱即用
- ✅ 完整的 uni-app 开发工具链
- ✅ 支持一键运行到浏览器、微信小程序、App等
- ✅ 内置代码提示和调试工具
- ✅ 热更新，保存即刷新

命令行方式适合：
- 已经在使用其他 IDE（如 VS Code）
- 需要 CI/CD 自动化构建
- 更熟悉命令行开发流程

