# 微信登录实现说明

## 如何获取微信小程序 AppID 和 AppSecret

### 步骤 1：注册微信小程序账号

1. 访问 [微信公众平台](https://mp.weixin.qq.com/)
2. 点击右上角"立即注册"
3. 选择"小程序"
4. 按照提示填写信息完成注册

### 步骤 2：登录微信公众平台

1. 访问 [微信公众平台](https://mp.weixin.qq.com/)
2. 使用注册的账号登录

### 步骤 3：获取 AppID（小程序ID）

1. 登录后，在左侧菜单找到 **"开发"** → **"开发管理"**
2. 或者直接访问：`https://mp.weixin.qq.com/cgi-bin/home?t=home/index&lang=zh_CN&token=你的token`
3. 在页面中找到 **"开发管理"** → **"开发设置"**
4. 在 **"开发者ID(AppID)"** 部分可以看到你的 **AppID**
   - 格式类似：`wx1234567890abcdef`
   - 这是公开的，可以放在前端代码中

### 步骤 4：获取 AppSecret（小程序密钥）

1. 在同一个 **"开发设置"** 页面
2. 找到 **"开发者密钥(AppSecret)"** 部分
3. 点击 **"生成"** 或 **"重置"** 按钮
4. 会弹出二维码，需要用管理员微信扫码确认
5. 确认后，会显示 **AppSecret**
   - 格式类似：`abcdef1234567890abcdef1234567890`
   - ⚠️ **重要：这个密钥只能查看一次，请立即保存！**
   - ⚠️ **如果忘记，需要重置，重置后旧的密钥会失效**

### 步骤 5：配置服务器域名（重要）

1. 在 **"开发设置"** 页面
2. 找到 **"服务器域名"** 部分
3. 点击 **"修改"** 按钮
4. 添加你的后端服务器域名：
   - **request合法域名**：添加你的后端 API 地址
     - 例如：`https://api.yourdomain.com`
     - 或者：`http://192.168.3.51:8080`（开发环境）
   - ⚠️ **注意：**
     - 不能使用 `localhost` 或 `127.0.0.1`
     - 必须使用 `https`（正式环境）或配置开发环境域名
     - 域名需要备案（正式环境）

### 步骤 6：配置到项目中

#### 方式一：后端配置文件（推荐）

在 `backend/src/main/resources/application.yml` 中配置：

```yaml
wechat:
  miniapp:
    appid: wx1234567890abcdef  # 你的 AppID
    secret: abcdef1234567890abcdef1234567890  # 你的 AppSecret
```

#### 方式二：环境变量（更安全）

在服务器上设置环境变量：

```bash
export WECHAT_APPID=wx1234567890abcdef
export WECHAT_SECRET=abcdef1234567890abcdef1234567890
```

然后在代码中读取：

```java
@Value("${wechat.miniapp.appid}")
private String appId;

@Value("${wechat.miniapp.secret}")
private String appSecret;
```

### 步骤 7：配置 manifest.json（前端）

在 `manifest.json` 中配置小程序 AppID：

```json
{
  "mp-weixin": {
    "appid": "wx1234567890abcdef",  // 你的 AppID
    "setting": {
      "urlCheck": false  // 开发环境可以关闭域名校验
    }
  }
}
```

### 注意事项

1. **AppSecret 保密**
   - ⚠️ 永远不要提交到 Git 仓库
   - ⚠️ 不要放在前端代码中
   - ⚠️ 只能在后端服务器使用
   - 建议使用环境变量或配置文件（不提交到 Git）

2. **开发环境配置**
   - 开发时可以在微信开发者工具中关闭域名校验
   - 在 `manifest.json` 中设置 `urlCheck: false`
   - 在微信开发者工具中：**设置 → 项目设置 → 不校验合法域名**

3. **正式环境配置**
   - 必须配置合法的服务器域名
   - 必须使用 HTTPS
   - 域名必须备案

4. **测试账号**
   - 如果没有正式的小程序账号，可以使用测试号
   - 测试号功能有限，建议注册正式账号

### 快速访问链接

- **微信公众平台登录：** https://mp.weixin.qq.com/
- **开发设置页面：** https://mp.weixin.qq.com/cgi-bin/home?t=home/index&lang=zh_CN

### 常见问题

**Q: AppSecret 忘记了怎么办？**
A: 在"开发设置"页面点击"重置"，用管理员微信扫码后重新生成。注意：旧的 AppSecret 会立即失效。

**Q: 可以使用测试号吗？**
A: 可以，但测试号功能有限，建议注册正式小程序账号。

**Q: 开发环境可以使用 localhost 吗？**
A: 不可以。需要使用局域网 IP（如 `192.168.x.x`）或配置开发域名。

**Q: 如何查看 AppID？**
A: 在微信公众平台 → 开发 → 开发管理 → 开发设置 → 开发者ID(AppID)

---

## 前端实现流程

### 1. 用户点击"微信一键登录"按钮

```javascript
handleWechatLogin() {
  // 检查是否同意协议
  if (!this.termsAgreed) {
    uni.showToast({
      title: '请先阅读并同意用户协议和隐私政策',
      icon: 'none'
    })
    return
  }
  
  // 调用微信登录
  // ...
}
```

### 2. 调用 uni.login 获取微信 code

```javascript
uni.login({
  provider: 'weixin',
  success: (res) => {
    // res.code 是微信登录凭证，有效期5分钟
    console.log('微信 code:', res.code)
  }
})
```

**重要说明：**
- `uni.login` 只能在**微信小程序环境**中使用
- 在 H5 环境下会失败，需要使用其他登录方式
- `code` 只能使用一次，且有效期只有 5 分钟

### 3. 将 code 发送到后端

```javascript
const response = await api.user.login(loginRes.code)
```

**API 接口：** `POST /api/user/login`

**请求参数：**
```json
{
  "code": "微信登录凭证"
}
```

### 4. 后端处理流程

后端需要完成以下步骤：

1. **接收前端传来的 code**
2. **调用微信接口换取 openid 和 session_key**
   ```
   GET https://api.weixin.qq.com/sns/jscode2session
   参数：
   - appid: 小程序 AppID
   - secret: 小程序 AppSecret
   - js_code: 前端传来的 code
   - grant_type: authorization_code
   ```
3. **根据 openid 查询或创建用户**
   - 如果用户已存在，更新登录信息
   - 如果用户不存在，创建新用户
4. **生成 JWT token 或其他认证令牌**
5. **返回 token 和用户信息给前端**

**后端响应格式：**
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "JWT_TOKEN_字符串",
    "userInfo": {
      "id": 1,
      "openid": "微信openid",
      "nickname": "微信昵称",
      "avatarUrl": "头像URL",
      "phone": "手机号（可能为空）"
    }
  }
}
```

### 5. 前端保存 token 和用户信息

```javascript
if (response.data && response.data.token) {
  uni.setStorageSync('token', response.data.token)
}

if (response.data && response.data.userInfo) {
  uni.setStorageSync('userInfo', response.data.userInfo)
}
```

### 6. 跳转到首页

```javascript
uni.reLaunch({
  url: '/pages/index/index'
})
```

## 后端实现示例（Java Spring Boot）

### 1. 添加微信登录接口

```java
@PostMapping("/login")
public Result login(@RequestBody Map<String, String> params) {
    String code = params.get("code");
    
    // 1. 调用微信接口获取 openid
    String openid = getWechatOpenid(code);
    
    // 2. 查询或创建用户
    User user = userService.findOrCreateByOpenid(openid);
    
    // 3. 生成 token
    String token = jwtUtil.generateToken(user.getId());
    
    // 4. 返回结果
    return Result.success(Map.of(
        "token", token,
        "userInfo", user
    ));
}

private String getWechatOpenid(String code) {
    // 调用微信接口
    String url = String.format(
        "https://api.weixin.qq.com/sns/jscode2session?appid=%s&secret=%s&js_code=%s&grant_type=authorization_code",
        appId, appSecret, code
    );
    
    // 使用 RestTemplate 或 HttpClient 发送请求
    // 解析返回的 JSON，获取 openid
    // ...
}
```

### 2. 配置小程序 AppID 和 AppSecret

在 `application.yml` 中配置：

```yaml
wechat:
  miniapp:
    appid: 你的小程序AppID
    secret: 你的小程序AppSecret
```

## 环境说明

### 微信小程序环境
- ✅ `uni.login({ provider: 'weixin' })` 可以正常使用
- ✅ 可以获取到有效的 code
- ✅ 后端可以正常换取 openid

### H5 环境（浏览器）
- ❌ `uni.login({ provider: 'weixin' })` 会失败
- ⚠️ 需要使用其他登录方式（如手机号登录）
- 💡 可以考虑使用微信 H5 授权登录（需要配置微信开放平台）

## 错误处理

### 常见错误及处理

1. **用户未同意协议**
   - 提示：请先阅读并同意用户协议和隐私政策

2. **uni.login 失败**
   - 可能原因：不在微信小程序环境
   - 处理：提示用户使用手机号登录

3. **code 过期或无效**
   - 后端返回错误
   - 前端重新调用 `uni.login` 获取新的 code

4. **网络错误**
   - 提示：网络连接失败，请检查网络

5. **后端登录失败**
   - 显示后端返回的错误信息

## 安全注意事项

1. **AppSecret 保密**
   - 永远不要在前端代码中暴露 AppSecret
   - AppSecret 只能在后端服务器使用

2. **Token 存储**
   - 使用 `uni.setStorageSync` 存储在本地
   - 在请求头中携带 token：`Authorization: Bearer {token}`

3. **Token 过期处理**
   - 后端返回 401 时，清除本地 token
   - 跳转到登录页面

4. **Code 一次性使用**
   - 每个 code 只能使用一次
   - 如果失败，需要重新获取 code

## 测试步骤

1. **在微信开发者工具中测试**
   - 打开微信开发者工具
   - 运行小程序
   - 点击"微信一键登录"按钮
   - 检查是否能正常获取 code 并登录

2. **检查 token 是否保存**
   ```javascript
   // 在控制台执行
   uni.getStorageSync('token')
   ```

3. **检查后续请求是否携带 token**
   - 查看网络请求的请求头
   - 确认 `Authorization` 字段存在

## 完整代码位置

- **前端登录页面：** `pages/login/login.vue`
- **API 请求封装：** `api/request.js`
- **后端接口：** `POST /api/user/login`

## 后续优化建议

1. **添加登录状态检查**
   - 在 App.vue 中检查 token 是否过期
   - 自动跳转到登录页

2. **添加用户信息获取**
   - 登录成功后，调用 `api.user.getInfo()` 获取完整用户信息
   - 更新本地存储的用户信息

3. **添加登录拦截**
   - 在需要登录的页面检查 token
   - 未登录时自动跳转到登录页

4. **添加 token 刷新机制**
   - 在 token 即将过期时自动刷新
   - 避免用户频繁登录

