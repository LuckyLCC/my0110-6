# 微信小程序启动步骤

## 方式一：使用 HBuilderX（最简单）⭐⭐⭐

### 步骤：

1. **安装微信开发者工具**
   - 下载地址：https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html
   - 下载并安装微信开发者工具

2. **在 HBuilderX 中启动**
   - 打开 HBuilderX
   - 确保项目已打开：`/Users/liuchang/Downloads/my0110-6`
   - 点击菜单：**运行 → 运行到小程序模拟器 → 微信开发者工具**
   - HBuilderX 会自动编译并打开微信开发者工具

3. **首次配置（如果提示）**
   - HBuilderX 会提示输入微信开发者工具的安装路径
   - macOS 默认路径：`/Applications/wechatwebdevtools.app`
   - 或者手动找到微信开发者工具的安装路径

4. **在微信开发者工具中查看**
   - 小程序会自动在微信开发者工具中打开
   - 可以看到完整的页面效果
   - 支持实时预览和调试

---

## 方式二：使用命令行

### 步骤：

#### 1. 编译小程序代码

```bash
cd /Users/liuchang/Downloads/my0110-6
npm run dev:mp-weixin
```

或者使用 uni-cli（如果已安装）：

```bash
npx @dcloudio/uvm
uni -p mp-weixin
```

#### 2. 打开微信开发者工具

1. 打开微信开发者工具
2. 选择 **导入项目** 或 **+** 新建项目
3. 项目目录选择：`/Users/liuchang/Downloads/my0110-6/unpackage/dist/dev/mp-weixin`
4. AppID：
   - 如果有正式 AppID：填写你的小程序 AppID（需要在 manifest.json 中配置）
   - 如果没有：选择 **测试号**（用于开发测试）
5. 项目名称：城市森林氧舱（或任意名称）
6. 点击 **导入** 或 **确定**

#### 3. 查看效果

- 小程序会在微信开发者工具中自动打开
- 可以预览首页、会员商城、预约、我的等页面
- 支持代码修改后自动编译刷新

---

## 注意事项

### 1. 配置 AppID（可选）

如果有正式的小程序 AppID，需要配置：

编辑 `manifest.json`：

```json
{
  "mp-weixin": {
    "appid": "你的小程序AppID",
    "setting": {
      "urlCheck": false
    },
    "usingComponents": true
  }
}
```

### 2. 测试号限制

使用测试号（touristappid）的限制：
- ✅ 可以正常开发调试
- ✅ 可以预览和体验
- ❌ 不能发布到线上
- ❌ 部分功能可能受限

### 3. 页面配置

所有页面已配置为 `navigationStyle: "custom"`（自定义导航栏），这样可以使用自定义的导航栏样式。

### 4. 底部导航

底部导航使用自定义组件实现，在小程序中会正常显示。

### 5. 图片路径

确保 `static` 目录下的图片路径正确：
- 本地图片使用 `/static/xxx.png` 格式
- 确保所有图片资源都在 `static` 目录中

---

## 常见问题

### Q1: 编译失败怎么办？

**A:** 检查依赖是否安装完整：
```bash
npm install --legacy-peer-deps
```

### Q2: 微信开发者工具提示找不到 AppID？

**A:** 选择 **测试号** 即可，不影响开发调试。

### Q3: 页面显示空白？

**A:** 检查：
1. 控制台是否有错误信息
2. 图片路径是否正确
3. 组件引入路径是否正确

### Q4: 底部导航不显示？

**A:** 检查：
1. `BottomNav.vue` 组件是否正确引入
2. 图片路径是否正确（`/static/` 开头）
3. 页面路径配置是否正确

### Q5: 样式显示不正确？

**A:** 
- uni-app 的 rpx 单位会自动转换为小程序单位
- 某些 CSS 属性可能不支持，需要调整
- 检查是否有样式冲突

---

## 推荐方式

**强烈推荐使用 HBuilderX**，因为：
- ✅ 一键编译并自动打开微信开发者工具
- ✅ 支持代码修改后自动重新编译
- ✅ 完整的调试和预览功能
- ✅ 无需手动配置路径

命令行方式适合：
- 已熟悉命令行操作
- 需要 CI/CD 自动化编译
- 需要在其他环境中使用


